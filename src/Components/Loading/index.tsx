import React from 'react'
import {
  LoadingSpinnerContainer,
  LoadingSpinner
} from './styled'

const Loading = () => (
  <LoadingSpinnerContainer>
    <LoadingSpinner />
  </LoadingSpinnerContainer>
)

export default Loading
